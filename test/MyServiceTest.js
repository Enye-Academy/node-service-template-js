import assert from 'assert';

const Service = require('../src/server.js');

// TODO: update your service tests
describe('Service', () => {
	it('should construct', () => {
		assert.strictEqual(!!Service, true);
	});
});
